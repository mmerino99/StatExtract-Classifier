# Core OCR & image processing
pytesseract>=0.3.10
opencv-python>=4.8.0
PyMuPDF>=1.23.0          # PDF -> image (no Poppler needed on Windows)
easyocr>=1.7.0           # deep-learning OCR — handles handwriting Tesseract cannot read

# NLP & ML
scikit-learn>=1.3.0
nltk>=3.8.1
numpy>=1.24.0

# Web server
flask>=3.0.0

# Utilities
tqdm>=4.65.0
