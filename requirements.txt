# ── OCR & image processing ────────────────────────────────────────────────────
pytesseract>=0.3.13
opencv-python>=4.13.0
PyMuPDF>=1.27.0          # native PDF text extraction + PDF-to-image rendering
easyocr>=1.7.0           # deep-learning fallback for handwriting / poor scans

# ── NLP & machine learning ────────────────────────────────────────────────────
scikit-learn>=1.3.0
nltk>=3.8.1
numpy>=1.24.0
spacy>=3.8.0             # NER for issuer/recipient extraction in invoices
# After installing spacy, download the language model:
#   python -m spacy download en_core_web_sm

# ── Web server ────────────────────────────────────────────────────────────────
flask>=3.0.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0
langdetect>=1.0.9        # language detection — flags non-English documents
